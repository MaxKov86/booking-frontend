# Адреса бекенду (booking-backend). Локально він за замовчуванням на :4000.
# NEXT_PUBLIC_ префікс обов'язковий — без нього змінна не потрапить
# у клієнтський бандл, і fetch з браузера не знатиме, куди йти.
NEXT_PUBLIC_API_URL=http://localhost:4000
